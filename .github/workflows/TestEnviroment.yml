name: Deployment

on:
  push:
    branches: develop
  
jobs:
  deployment:
    runs-on: ubuntu-latest
    environment:
      name: develop
      url: https://github.com
    steps:
      - name: develop
      
